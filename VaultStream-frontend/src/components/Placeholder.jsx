// Placeholder.jsx — generic stub for routes that are wired up but not yet built.

import React from 'react';

export default function Placeholder({ title, subtitle }) {
  return (
    <div className="pagehead">
      <h1>{title}</h1>
      <p>{subtitle}</p>
    </div>
  );
}
